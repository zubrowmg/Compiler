program VectorOps is

integer indx;
integer size;
char x;
float vect1[-5:5];
float vect2[-5:5];
float vect3[-2:8];
float vect4[0:10];
float vect5[10:20];

begin

size := 0;
for(indx := -5; indx < 4)
	size := size + 1;
	indx := indx + 1;
end for;





putChar('e');
for(indx := 10; indx < 20)
	putFloat(vect5[indx]);
	indx := indx + 1;
end for;

end program.

